export default function TaskDetailConceptS() {
  return (
    <div className="standalone-html-view">
      <iframe
        title="Concept S minimal task view"
        src={`${import.meta.env.BASE_URL}task-detail-concept-s.html`}
      />
    </div>
  );
}
